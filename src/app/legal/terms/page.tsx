import { APP_NAME } from "@/lib/legal/constants";

export default function TermsPage() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-16 prose">
      <h1>{APP_NAME} Terms of Service</h1>
      {/* TODO: fill in full terms before launch — subscription terms, cancellation, acceptable use */}
    </main>
  );
}
